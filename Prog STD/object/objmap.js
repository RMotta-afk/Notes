const pessoas = [
    {id:3, nome: 'Sung'},
    {id:2, nome: 'Marin'},
    {id:1, nome: 'Cássio'},
];

const newPeople = {};

for (const {id, nome} of pessoas) {
    const {id} = pessoa;
    newPeople[id] = { ...pesoas}
}